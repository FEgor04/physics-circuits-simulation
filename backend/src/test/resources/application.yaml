spring:
  datasource:
    username: sa
    password: sa
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver

security:
  jwt:
    secret: test_jwt_secret_AAAAAAAAAAAAAAAAAA
    access: 1000
    refresh: 10000
  admin:
    data:
      key: adminKey
      adminUsername: adminUsername
      adminPassword: admin
      adminName: adminName
